import { ShyriiwookPipe } from './shyriiwook.pipe';

describe('ShyriiwookPipe', () => {
  it('create an instance', () => {
    const pipe = new ShyriiwookPipe();
    expect(pipe).toBeTruthy();
  });
});
